"""Tests for the replay package. Run from the package directory:

    python3 -m pytest -p no:cacheprovider test -q

They need numpy and pytest only. ROS modules are stubbed where a node
module's pure helpers are under test.
"""
